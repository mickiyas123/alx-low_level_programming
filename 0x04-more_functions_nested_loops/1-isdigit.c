#include "main.h"

/**
 * _isdigit - Main Entry
 * Description: Checks for numeric character
 * @c: the character to be checked
 *
 * Return: 1 if c is digit else 0
 */
int _isdigit(int c)
{
	return (c >= 48 && c <= 57);
}
