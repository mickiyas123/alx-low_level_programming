#include "main.h"

/**
 * mul - Main Entry
 * Description: multiplies two numbers
 * @a: the first number
 * @b: the second number
 *
 * Return: multiplication of two number as a result
 */
int mul(int a, int b)
{
	return (a * b);
}
